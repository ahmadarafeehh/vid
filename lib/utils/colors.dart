import 'package:flutter/material.dart';

const webBackgroundColor = Color.fromRGBO(200, 200, 200, 1); // Medium gray
const mobileSearchColor =
    Color.fromRGBO(220, 220, 220, 1); // Slightly darker gray
const blueColor = Colors.grey; // Orange for accents

const secondaryColor = Colors.black; // 70% opacity black
const Color mobileBackgroundColor = Color(0xFF121212); // Dark background
const Color primaryColor = Color(0xFFd9d9d9); // Light gray for accents
